# Fallback server
ssh_user=yunouser
ssh_host=crudelis-test4.fr
ssh_key=ssh_key
ssh_port=
ssh_options=

# Encryption of the archives before they sent, default 0
encrypt=1
pass_file=cred

# Main backup directory
main_storage_dir=

# Overwrite server IP
overwrite_ip=
